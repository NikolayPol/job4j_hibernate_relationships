CREATE TABLE model (
    id SERIAL PRIMARY KEY,
    name TEXT
);

CREATE TABLE brand (
    id SERIAL PRIMARY KEY,
    name TEXT
);

CREATE TABLE authors (
    id SERIAL PRIMARY KEY,
    name TEXT
);

CREATE TABLE books (
    id SERIAL PRIMARY KEY,
    name TEXT
);